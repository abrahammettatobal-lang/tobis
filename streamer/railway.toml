[build]
builder = "NIXPACKS"
watchPatterns = ["server.js", "src/**", "package.json", "package-lock.json"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
