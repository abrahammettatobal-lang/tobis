[build]
builder = "NIXPACKS"
watchPatterns = ["src/**", "data/**", "package.json", "package-lock.json"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
